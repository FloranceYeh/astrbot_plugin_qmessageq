{
  "admin_only": {
    "description": "命令仅管理员可用",
    "type": "bool",
    "hint": "开启后，所有命令（himg、fake、at）仅对管理员生效；关闭后所有成员可用。",
    "default": true
  },
  "at_admin_only": {
    "description": "at 命令仅管理员可用",
    "type": "bool",
    "hint": "开启后，at 命令仅对管理员生效，独立于 admin_only；关闭后所有成员可用。",
    "default": true
  }
}
