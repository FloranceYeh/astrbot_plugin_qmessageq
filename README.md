<p align="center">
  <img src="logo.png" alt="QmessageQ Logo" width="180" />
</p>

<h1 align="center">QmessageQ</h1>

<p align="center">多功能 QQ 消息工具箱（适配 aiocqhttp / NapCat / OneBot v11）。</p>

## 功能

- **图片藏文案（`/himg`）**：把文案塞进图片消息的 `summary` 字段后重发，对端客户端显示的是文案而非 `[图片]` 占位。
- **转发消息伪装（`/fake`）**：发送合并转发消息，伪造节点的昵称与 QQ 号，还可用 `头像=` / `ava=` 让节点显示指定 QQ 号的头像；支持 `||` 分隔多条消息伪造一段群聊记录；消息附带图片时会伪装成第一个人发的图。
- **真 @（`/at`）**：发送真实的 @ 提及或 @全体成员；支持群昵称/群名片解析与随机 @ 多名成员。
- **名片（`/card`）**：发送指定 QQ 或群的真实推荐名片卡片。
- **回应表情（`/face`）**：引用一条消息后用表情回应（NapCat 的 `set_msg_emoji_like`），支持 `a-b` 范围一次回应多个、`NxM` 对单个表情点/取消交替轰炸， `/face stop` 中止。
- **发送表情（`/hface`）**：把内置表情（含默认面板不显示的隐藏表情）作为消息发送，支持 `a-b` 范围及 `/hface list` 分页查看隐藏表情。
- **解析引用消息（`/parse`）**：引用一条消息后解析其发送者、时间、内容、表情 ID、图片 summary、表情回应与合并转发内容（以合并转发消息分节点返回）。
- **魔法表情（`/magic`）**：输出 QQ 以 `<$...>` 文本模板内嵌的「魔法表情」——`/magic <数字序列>` 按码点构造（如 `/magic 255 256 17 16`），或引用含魔法表情的消息自动提取重发。
- **LLM @ 工具（`at_user`）**：让 LLM 在回复开头 @ 指定用户或全体成员，目标支持群昵称/群名片、QQ 号或 `all`。

## 配置

在 AstrBot WebUI 的插件配置中设置：

- `admin_only`：全局兜底开关，作为各命令未单独配置时的默认值，默认开启。
- `himg_admin_only`：`himg` 命令仅管理员可用。
- `fake_admin_only`：`fake` 命令仅管理员可用。
- `at_admin_only`：`at` 命令仅管理员可用。
- `card_admin_only`：`card` 命令仅管理员可用。
- `face_admin_only`：`face` 命令仅管理员可用。
- `face_interval`：`face` 范围回应时每个表情之间的间隔秒数（默认 `1`，设为 `0` 则不停顿）。
- `hface_admin_only`：`hface` 命令仅管理员可用。
- `at_user_admin_only`：LLM `at_user` 工具仅管理员可用。
- `parse_admin_only`：`parse` 命令仅管理员可用。
- `magic_admin_only`：`magic` 命令仅管理员可用。

各命令的 `<命令名>_admin_only` 均默认开启，未设置时回退到 `admin_only`。修改配置后请重载插件。

## 命令

每个命令的权限由各自的 `<命令名>_admin_only` 配置单独控制，未配置时回退到 `admin_only`（默认均仅管理员可用）。

### 图片藏文案

```
/himg <文案>
```
发送时在同一消息里附一张图片，bot 会重发该图片并把文案写入 `summary` 字段。引用他人带图片的消息时，也会取用被引用消息里的图片。

```
/himg <文案> <图片URL>
```
不带附件时，末尾参数若是 `http(s)://` 链接则作为图片来源。

### 转发消息伪装

```
/fake <昵称> <QQ号> <文本>
```
发送一条包含单个节点的合并转发消息，节点昵称与 QQ 号为指定值。命令附带图片时，图片会作为该人发出的图片放进节点内容。

多条消息用 `||` 分隔，伪造一整段群聊记录：

```
/fake 张三 10001 第一条消息 || 李四 10002 第二条消息 || 张三 10001 第三条消息
```
每条 `||` 段格式同为 `<昵称> <QQ号> <文本>`，文本中请勿包含 `||`。

自定义头像：在节点文本末尾追加 `头像=<QQ号>`（别名 `ava=`），该节点的发送人 QQ 会替换为指定值，NapCat 按节点的 `user_id` 渲染头像，因此会显示该 QQ 的头像：

```
/fake 张三 10001 你好 ava=3393314989
```
昵称仍为「张三」，头像为 3393314989 的头像。NapCat 无法渲染自定义头像 URL，填 URL 会报错。文本中请勿以 `头像=` / `ava=` 结尾。

### 真 @

```
/at <QQ号|all|random> <文本>
```
- `qq=all` 时发送 @全体成员（仅群聊有效）。
- `qq` 为群成员昵称/群名片时，会自动解析为对应 QQ 号再 @（仅群聊有效）。
- `qq=random`（或 `r`）时随机 @ 群成员，`文本` 开头为数字则视为 @ 人数，如 `/at random 3 开黑` 随机 @ 3 人（默认 1 人）。

### 名片

```
/card qq <QQ号>
/card group <群号>
```
发送该 QQ 或该群的真实推荐名片卡片（NapCat 按目标 ID 生成，ID 必须真实存在）。

### 回应表情

先引用（回复）一条消息，再执行：

```
/face <表情>
```

`<表情>` 可以是表情名（如 `微笑`）、英文别名（如 `smile`）、数字表情 ID（如 `14`）、`#` 加数字（如 `#14`）、数字范围 `a-b`（如 `/face 66-70` 一次回应 66~70 号表情）、重复 `NxM`（如 `/face 1x20` 对 1 号表情**点/取消交替轰炸 20 次**，最多 100 次），或一个 Emoji（可叠加 `xM`，如 `/face 💢x5`）。由于同一个账号对同一表情只能有一个回应，`NxM` 通过交替「点一次、取消一次」的方式让该表情反复闪现。

范围回应时按 `face_interval`（默认 1 秒）逐个添加，bot 会先发一条「预计耗时」提示，结束后若有失败的表情会汇总成一条失败列表（不会每个失败都单独发一条）。范围进行中可执行 `/face stop` 中止（按会话区分，会在下一个间隔内停下，并提示已完成数量）。

追加参数：

- `cancel`：取消该表情回应，如 `/face 爱心 cancel`（需要指定要取消的表情）。
- `big`：发送大表情，如 `/face 爱心 big`（仅 `set_msg_reaction` 实现支持）。

Emoji 回应通过 NapCat `set_msg_emoji_like` 的 Unicode 类型实现，无需任何映射表；若你的协议端不支持则报错。

表情编号以 NapCat 的 QSid 为准（见 `[FACES.md](./FACES.md)` 大全），与经典 go-cqhttp 的 CQ face 编号不一致。常用示例：`微笑`(14)、`得意`(4)、`流泪`(5)、`大哭`(9)、`笑哭`(182)、`鼓掌`(99)、`左亲亲`(109)、`玫瑰`(63)、`爱心`(66)、`心碎`(67)、`蛋糕`(53)、`炸弹`(55)、`便便`(59)、`拥抱`(49)、`赞`(76)、`OK`(124)、`飞吻`(85)、`挥手`(129)、`骰子`(358)。

### 发送表情

```
/hface <名称|ID>
/hface <名称|ID> 文本
/hface 1-5
/hface list
/hface text <内容>
```

`<名称|ID>` 支持名字 / 英文别名 / 数字 ID / `#ID` / 范围 `a-b`。`/hface` 把内置表情作为普通消息发出，主要用于发送默认表情面板不显示的**隐藏表情**，如 `/hface 骰子`、`/hface 358`；`/hface list [a-b]` 以**合并转发消息**形式分页查看隐藏表情（默认第 1-20 个，每次最多 20 个，如 `/hface list 21-40` 看下一页），每个节点是一个表情及其名称，完整映射见插件目录的 `FACES.md`。

### 解析引用消息

```
/parse
```

引用（回复）一条消息后执行 `/parse`，bot 会以**合并转发消息**的形式返回，每个部分一个节点：

- **基本信息**：消息 ID、发送者。
- **时间**：发送时间。
- **内容**：文本、消息内容里的**表情 ID**、@。
- **图片summary**：图片的 `summary` 字段与 url（可用来查看 `/himg` 藏进图片的文案）。
- **回应**：该消息收到的**表情回应**（`emoji_id`、type、数量）。
- **转发**：若消息是**合并转发**，展开每条节点的发送者与内容（最多 30 条）。

### 魔法表情

QQ 的「魔法表情/动态表情」不通过 face 段下发，而是以内嵌在文本里的 `<$...>` 模板表示，客户端渲染时替换成表情。模板含不可见控制字符，复制会丢，因此用 `/magic` 按数字序列构造或从真实消息提取，不做内置目录、不自动收录：

```
/magic 178             # 单个 → <$chr(178)> = <$²>（滑稽）
/magic 255 256 17 16   # 多字段 → <$ÿĀ\x11\x10>（摇手手）
/magic 255 256 17 1-32 # 字段支持 a-b 范围，展开成笛卡尔积一起发出（最多 100 个）
/magic 0x42 0x23       # 数字支持十进制或 0x 十六进制（0x42=66，0x23=35）
/magic <模板>          # 把模板原样发出（客户端渲染成表情）
/magic                 # 引用含魔法表情的消息 → 解析成码点序列输出：magic 255 256 17 16 | 0xff 0x100 0x11 0x10
```

魔法表情的规律：`<$...>` 内是一串字符码。**单字符族 `<$X>` 渲染的就是 face ID 为 X 的表情**（`滑稽`=`<$²>`，²=U+00B2=178），所以 `/magic 178` 与 face ID 178 等价。多字符族目前发现两种基座 `ÿ`(0x00FF=255)/`ǿ`(0x01FF=511) + `Ā`(0x0100=256) + 两个参数，基座可能不止这两种。

## LLM 工具

工具名：`at_user`

参数：

- `target`（string）：要 @ 的目标，可以是群成员的昵称/群名片、QQ 号，或 `all`。
- `at_all`（boolean，默认 `false`）：为 `true` 时 @ 全体成员并忽略 `target`。

工具内部先按当前群成员昵称/名片匹配 QQ 号，匹配不到时按 QQ 号原样使用。记录的 @ 目标会插入到 LLM 回复的开头。

## 说明

- 命令名固定为 `himg` / `fake` / `at` / `card` / `face` / `hface`（AstrBot 的 `@filter.command` 在导入时注册，暂不支持按配置动态改名）。
- `himg` 依赖协议端透传图片段 `summary` 字段（NapCat / LLOneBot 支持），实际渲染效果请以你的客户端为准。
- `fake` 节点的头像由节点的发送人 QQ 号决定（NapCat 按 `user_id` 渲染），`头像=` / `ava=` 实质是替换节点的发送人 QQ。
- `face` 依赖协议端的回应动作：NapCat 使用 `set_msg_emoji_like`，LLOneBot 等实现使用 `set_msg_reaction`，插件会自动回退。
- 表情编号以 NapCat 的 QSid 为准，映射表与隐藏表情大全见插件目录的 `FACES.md`（数据与代码见 `faces.py`）。
- 仅支持 aiocqhttp（OneBot v11）平台。

## 声明

本插件仅供交流学习使用，请勿用于任何不正当用途（如诈骗、冒充他人、散布虚假信息等），由此产生的一切后果与作者无关。
